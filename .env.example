# ========================================
# OpenAI API Configuration
# ========================================
# REQUIRED: API key for Azure OpenAI
OPENAI_API_KEY=your_api_key_here

# REQUIRED: Custom OpenAI API endpoint
# For Azure OpenAI: https://{resource-name}.openai.azure.com/openai/deployments/{deployment-name}
OPENAI_BASE_URL=https://this-is-a-fantastic-resource.openai.azure.com/openai/deployments/gpt-4o

# REQUIRED: Custom query parameters if applicable
# For Azure OpenAI: api-version=YYYY-MM-DD
OPENAI_QUERY_PARAMS=api-version=2025-11-23

# REQUIRED: Inference model name (used for API calls)
# For Azure OpenAI: use the deployment name configured in Azure (here: gpt-4o)
OPENAI_INFERENCE_MODEL_NAME=gpt-4o

# ========================================
# Document Processing Configuration
# ========================================
# REQUIRED: Size of text chunks for processing in characters (same as in paper)
CHUNK_SIZE=600

# REQUIRED: Overlap between chunks to preserve context in characters (same as in paper)
CHUNK_OVERLAP=100

# ========================================
# Rate Limiting Configuration
# ========================================
# REQUIRED: Token bucket identifier for rate limiting
RATE_LIMIT_BUCKET_ID=default

# REQUIRED: Maximum capacity of the token bucket
RATE_LIMIT_CAPACITY=128000

# REQUIRED: Token refill rate per second (e.g., 2133.33 tokens/sec for 128,000 tokens/min)
RATE_LIMIT_REFILL_RATE=2133.33

# REQUIRED: Timeout for rate limit operations in seconds
RATE_LIMIT_TIMEOUT=65.0

# ========================================
# Neo4j Database Configuration
# ========================================
# Neo4j username (default: neo4j)
NEO4J_USERNAME=neo4j

# Neo4j password (default: neo4j_password)
NEO4J_PASSWORD=neo4j_password

# Neo4j database name (default: neo4j)
NEO4J_DATABASE=neo4j

# ========================================
# Redis Configuration
# ========================================
# Redis host (default: redis for Docker, localhost for local dev)
REDIS_HOST=redis

# Redis port (default: 6379)
REDIS_PORT=6379

# ========================================
# Logging Configuration
# ========================================
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
LOG_LEVEL=INFO